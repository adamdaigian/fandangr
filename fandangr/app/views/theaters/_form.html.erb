<%= form_for(@theater) do |f| %>
  <% if @theater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@theater.errors.count, "error") %> prohibited this theater from being saved:</h2>

      <ul>
      <% @theater.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %><br />
    <%= f.text_field :location %>
  </div>
  <div class="field">
    <%= f.label :screen %><br />
    <%= f.number_field :screen %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= form_for @theater, :url => theaters_path, :method => "post" do |f| %>

  <div>
  <%= f.label :theater_id %>
  <%= f.collection_select(:theater_id, Theater.all, :id, :name, {:prompt => "select theater"}) %>
  </div>

  <div>
  <%= f.button %>
  </div>
<% end %>
